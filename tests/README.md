# Shared Tests

This root test area is reserved for cross-service contract, authoritative media-binding, and end-to-end scenarios. Unit tests remain with `backend/` or `ml/`.

Phase C adds no shared product test because no cross-service workflow exists yet. Repository-boundary checks live under `infrastructure/checks/` because they validate repository ownership rather than product behavior.

